# OS
.DS_Store
Thumbs.db

# Editor
*.swp
*.swo
.idea/

# VS Code — keep shared settings, ignore user-specific
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json

# Node / build
node_modules/
dist/
build/
.cache/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env / secrets
.env
.env.*
!.env.example

# Spec Kit
# Keep .specify/ and .github/ committed (templates, prompts, agents, memory).
# Only ignore runtime/transient files if the CLI writes any.
.specify/cache/
